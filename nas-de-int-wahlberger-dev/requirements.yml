---
# Galaxy content this playbook depends on. Install before the first run:
#
#   ansible-galaxy collection install -r requirements.yml
#
# Versions are pinned with a floor (">=") so installs are reproducible but can
# still pick up patch/minor fixes. Bump deliberately.
collections:
  # zypper / zypper_repository / timezone modules, used by
  # common/podman/firewall/snapper.
  - name: community.general
    version: ">=8.0.0"

  # profile_tasks callback wired up in ansible.cfg, plus the firewalld module
  # used by the firewall role and the mount module used by the storage role.
  # Floor is 2.2.2, not just "newer": 1.5.4 (what >=1.5.0 had resolved to
  # and stayed pinned at) still imports from the ansible-core paths that
  # ansible-core 2.20+ deprecates (to_native/to_bytes location, exit_json's
  # warnings kwarg) and its profile_tasks callback hadn't yet implemented
  # the v2_* method ansible-core 2.20+ wants — 2.2.2 fixes all of it.
  - name: ansible.posix
    version: ">=2.2.2"

  # podman_network module, used by the paperless_ngx role to isolate its
  # redis/postgres backend on their own network.
  - name: containers.podman
    version: ">=1.16.0"
