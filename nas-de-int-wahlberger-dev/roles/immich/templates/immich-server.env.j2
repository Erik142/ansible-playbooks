{# Rendered by the immich role to {{ immich_data_dir }}/immich-server.env (0600). #}
{# Keeps the Postgres password out of the world-readable Quadlet unit. Must #}
{# match POSTGRES_PASSWORD in immich-postgres.env. Do NOT edit on the host. #}
DB_PASSWORD={{ immich_db_password }}
