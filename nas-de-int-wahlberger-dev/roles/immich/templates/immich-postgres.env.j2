{# Rendered by the immich role to {{ immich_data_dir }}/immich-postgres.env (0600). #}
{# Keeps the Postgres password out of the world-readable Quadlet unit. Must #}
{# match DB_PASSWORD in immich-server.env. Do NOT edit on the host. #}
POSTGRES_PASSWORD={{ immich_db_password }}
