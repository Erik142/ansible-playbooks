{# Rendered by the paperless_ngx role to {{ paperless_ngx_db_data_dir_root }}/paperless-db.env (0600). #}
{# Keeps the Postgres password out of the world-readable Quadlet unit. Must #}
{# match PAPERLESS_DBPASS in paperless-ngx.env. Do NOT edit on the host. #}
POSTGRES_PASSWORD={{ paperless_ngx_db_password }}
