---
- name: Restart samba
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: restarted
  loop:
    - smb
    - nmb

- name: Reload the systemd daemon
  ansible.builtin.systemd:
    daemon_reload: true
