# Rendered by the restic_backup role. Root-only — read by the backup script
# via `podman exec --env-file` so PGPASSWORD never appears in `ps` output or
# a command-line argument. Do NOT edit on the host — re-run Ansible.
PGPASSWORD={{ paperless_ngx_db_password }}
