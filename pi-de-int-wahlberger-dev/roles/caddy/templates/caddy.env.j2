{# Rendered by the caddy role to {{ caddy_data_dir }}/caddy.env (0600). #}
{# Keeps the Cloudflare DNS API token out of the world-readable Quadlet unit #}
{# and out of the Caddyfile itself. Do NOT edit on the host. #}
CLOUDFLARE_API_TOKEN={{ cloudflare_dns_api_token }}
