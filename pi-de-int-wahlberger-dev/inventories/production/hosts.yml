---
# Production inventory.
#
# This file holds only the host/group *structure*. All variables (connection
# details, role settings, secrets) live in group_vars/ so that "what exists"
# stays separate from "how it's configured".
all:
  children:
    pi:
      hosts:
        pi.de.int.wahlberger.dev:
